import React from 'react';
import '../styles/cards.css'

const Card = () => {
    return (
        <div
            id="card"
            className="bg-primary"
        >
            hello
        </div>
    );
};

export default Card;
