import React from 'react';

import '../styles/cards.css'

const TitleCard = () => {
    return (
        <div
            id="title-card"
            className="bg-primary"
        >

        </div>
    );
};

export default TitleCard;
